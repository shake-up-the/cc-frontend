import 'package:flutter/material.dart';

class DictRoomView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('사전방'),
      ),
    );
  }
}
